# =============================================================================
# Sameer Saifi Portfolio (SamX) — Environment Variables
# Copy to .env.local for local development.
# Set these in Vercel for production deployments.
# =============================================================================

# -----------------------------------------------------------------------------
# Site
# -----------------------------------------------------------------------------
NEXT_PUBLIC_BASE_URL=http://localhost:3000

# -----------------------------------------------------------------------------
# Admin / route protection (optional)
# Used by /api/authenticate for password-protected pages (see once-ui.config.ts)
# -----------------------------------------------------------------------------
ADMIN_EMAIL=sameer1x9@gmail.com
ADMIN_PASSWORD=your-admin-password

# -----------------------------------------------------------------------------
# SyncApp blog integration (optional, server-side only)
# -----------------------------------------------------------------------------
# SYNCAPP_API_URL=
# SYNCAPP_API_TOKEN=
# SYNCAPP_EMAIL=
# SYNCAPP_PASSWORD=
